{
    "name": "vanjacloud.remote",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "devDependencies": {
        "@types/bun": "latest",
        "typescript": "^5.3.3"
    },
    "scripts": {
        "start": "bun run index.ts",
        "build": "bun build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "dotenv": "^16.4.2"
    }
}
